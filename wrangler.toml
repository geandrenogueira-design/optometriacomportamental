name = "appoptometry"
compatibility_date = "2026-02-26"

# Cloudflare Pages output directory
pages_build_output_dir = "public"

# Bind a KV namespace named OPTO_KV (create it in Cloudflare dashboard or with wrangler)
# kv_namespaces = [
#   { binding = "OPTO_KV", id = "YOUR_KV_NAMESPACE_ID" }
# ]
